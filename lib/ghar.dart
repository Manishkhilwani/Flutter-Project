import 'package:flutter/material.dart';
// import 'main.dart';

class sell extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sell'),
      ),
      body: Center(
        child: ElevatedButton(
          onPressed: () {},
          child: Text('No Way Home'),
        ),
      ),
    );
  }
}
